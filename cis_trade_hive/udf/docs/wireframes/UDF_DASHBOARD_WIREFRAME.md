# UDF Dashboard Wireframe

## Page: `/udf/`

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                          UDF Dashboard                                       │
│  Manage user-defined fields across all modules                              │
│                                                    [Add Field Value] [View All]│
└─────────────────────────────────────────────────────────────────────────────┘

┌─────────────────────┐  ┌─────────────────────┐  ┌─────────────────────┐
│  💼 Portfolio       │  │  📈 Equity Price    │  │  🛡️ Security        │
│  PORTFOLIO          │  │  EQUITY_PRICE       │  │  SECURITY           │
│                     │  │                     │  │                     │
│  Total: [5]         │  │  Total: [12]        │  │  Total: [8]         │
│                     │  │                     │  │                     │
│  ┌───────┬────────┐ │  │  ┌───────┬────────┐ │  │  ┌───────┬────────┐ │
│  │Active │Inactive│ │  │  │Active │Inactive│ │  │  │Active │Inactive│ │
│  │  [3]  │  [2]   │ │  │  │ [10]  │  [2]   │ │  │  │  [6]  │  [2]   │ │
│  └───────┴────────┘ │  │  └───────┴────────┘ │  │  └───────┴────────┘ │
│                     │  │                     │  │                     │
│  [View Fields]      │  │  [View Fields]      │  │  [View Fields]      │
└─────────────────────┘  └─────────────────────┘  └─────────────────────┘

┌─────────────────────┐  ┌─────────────────────┐  ┌─────────────────────┐
│  ↔️ Trade           │  │  📊 Market Data     │  │  📖 Reference       │
│  TRADE              │  │  MARKETDATA         │  │  REFERENCE          │
│                     │  │                     │  │                     │
│  Total: [0]         │  │  Total: [15]        │  │  Total: [3]         │
│                     │  │                     │  │                     │
│  ┌───────┬────────┐ │  │  ┌───────┬────────┐ │  │  ┌───────┬────────┐ │
│  │Active │Inactive│ │  │  │Active │Inactive│ │  │  │Active │Inactive│ │
│  │  [0]  │  [0]   │ │  │  │ [13]  │  [2]   │ │  │  │  [3]  │  [0]   │ │
│  └───────┴────────┘ │  │  └───────┴────────┘ │  │  └───────┴────────┘ │
│                     │  │                     │  │                     │
│  [View Fields]      │  │  [View Fields]      │  │  [View Fields]      │
└─────────────────────┘  └─────────────────────┘  └─────────────────────┘
```

---

## Features:

1. **Module Cards**
   - Color-coded icons for visual distinction
   - User-friendly names (Portfolio, Equity Price, etc.)
   - Technical codes shown below (PORTFOLIO, EQUITY_PRICE, etc.)
   - Field statistics (Total, Active, Inactive)
   - "View Fields" button → Navigates to filtered list

2. **Header Actions**
   - "Add Field Value" → Create new field value
   - "View All" → View complete list without filters

3. **Click Actions**
   - Clicking "View Fields" on Portfolio card:
     ```
     → Navigates to: /udf/list/?object_type=PORTFOLIO&status=active
     → List page opens with pre-selected filters
     ```

---

## Card Color Scheme:

| Module        | Icon | Color   |
|---------------|------|---------|
| Portfolio     | 💼   | Blue    |
| Equity Price  | 📈   | Green   |
| Security      | 🛡️   | Yellow  |
| Trade         | ↔️   | Cyan    |
| Market Data   | 📊   | Red     |
| Reference     | 📖   | Gray    |
| Comments      | 💬   | Blue    |
| Position      | 📍   | Green   |

---

## Data Flow:

```
Dashboard Load
    ↓
GET /udf/
    ↓
Backend: udf_field_service.get_dashboard_stats()
    ↓
SQL: SELECT object_type, COUNT(*) as total_fields,
     SUM(CASE WHEN is_active = true THEN 1 ELSE 0 END) as active_fields,
     SUM(CASE WHEN is_active = false THEN 1 ELSE 0 END) as inactive_fields
     FROM cis_udf_field
     GROUP BY object_type
    ↓
Render cards with statistics
```
