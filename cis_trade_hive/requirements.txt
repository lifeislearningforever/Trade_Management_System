# Core Django
Django==5.2.9
django-extensions==4.1

# WebSocket / Real-time notifications
channels==4.2.0              # Django Channels WebSocket support
channels-redis==4.2.1        # Redis channel layer (production)
daphne==4.1.2                # ASGI server (WebSocket support)
uvicorn[standard]==0.30.6   # ASGI worker for gunicorn (WebSocket support)
redis==5.2.1                 # Redis client (required by channels-redis)
pytest-asyncio==0.24.0       # Async test support for Channels consumers

# Database - Hive Connection
PyMySQL==1.1.1
PyHive==0.7.0
#sasl==0.3.1
thrift==0.16.0
thrift-sasl==0.4.3

# REST API
djangorestframework==3.16.1
django-filter==25.2

# Forms
django-crispy-forms==2.5
crispy-bootstrap5==2025.6

# Images
Pillow==12.0.0

# Testing
pytest==8.3.4
pytest-django==4.11.1
pytest-cov==7.0.0

# Environment
python-dotenv==1.0.1

# Additional utilities
whitenoise==6.7.0
gunicorn==22.0.0

# File Upload & Processing
chardet>=5.0.0           # Character encoding detection
openpyxl>=3.1.0          # Excel file support (.xlsx)
pyarrow>=14.0.0          # Parquet file support
